<Window x:Class="PausaVital.Views.ModeSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="/Assets/Icons/PausaVital.ico"
        Title="Seleccionar modo de trabajo"
        Height="280"
        Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F0F4F8"
        ShowInTaskbar="False"
        Topmost="True">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Elige tu modo de concentración"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="#2C3E50"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,5"/>

        <TextBlock Grid.Row="1"
                   Text="Selecciona cómo deseas que Pausa Vital administre tus descansos."
                   Foreground="#7F8C8D"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"
                   TextWrapping="Wrap"
                   Margin="0,0,0,20"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Background="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="OnRule20Clicked">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="10"/>
                    </Style>
                </Button.Resources>
                <Button.Effect>
                    <DropShadowEffect Color="Black"
                                      Opacity="0.05"
                                      BlurRadius="10"
                                      ShadowDepth="2"/>
                </Button.Effect>
                <StackPanel Margin="15">
                    <TextBlock Text="Regla 20-20-20"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="#2980B9"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="20 minutos de trabajo"
                               FontSize="12"
                               Foreground="#95A5A6"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,2"/>
                    <TextBlock Text="20 segundos de descanso"
                               FontSize="12"
                               Foreground="#95A5A6"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Grid.Column="2"
                    Background="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="OnPomodoroClicked">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="10"/>
                    </Style>
                </Button.Resources>
                <Button.Effect>
                    <DropShadowEffect Color="Black"
                                      Opacity="0.05"
                                      BlurRadius="10"
                                      ShadowDepth="2"/>
                </Button.Effect>
                <StackPanel Margin="15">
                    <TextBlock Text="Pomodoro"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="#E74C3C"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="25 minutos de trabajo"
                               FontSize="12"
                               Foreground="#95A5A6"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,2"/>
                    <TextBlock Text="5 minutos de descanso"
                               FontSize="12"
                               Foreground="#95A5A6"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Window>
