<Window x:Class="PausaVital.Views.ClosePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Exit Pausa Vital" Height="220" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="#F0F4F8"
        ShowInTaskbar="False" Topmost="True">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="How do you want to close the app?" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="1" Text="You can keep Pausa Vital running in the background to maintain your streaks, or exit completely." TextWrapping="Wrap" Foreground="#7F8C8D" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="2" VerticalAlignment="Bottom">
            <CheckBox Name="RememberCheckBox" Content="Remember my choice and don't ask again" Foreground="#34495E" Margin="0,0,0,15"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Content="Minimize to Tray" Background="#2980B9" Foreground="White" BorderThickness="0" Padding="10" Cursor="Hand" Click="OnMinimizeClicked">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Grid.Column="2" Content="Exit Completely" Background="#E74C3C" Foreground="White" BorderThickness="0" Padding="10" Cursor="Hand" Click="OnExitClicked">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </StackPanel>
    </Grid>
</Window>